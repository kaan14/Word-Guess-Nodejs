//discribe the game rules. for example; console.log("to start, enter "node letter.js start" + "\n\n you have to guess the leeters of the displayed word
// if you run out of guess #s, then you loose, otherwise you win. Lets type "node letter.js start" ")
console.log("to start, enter node letter.js start" + "\n\n you have to guess the leeters of the displayed word if you run out of guess #s, then you loose, otherwise you win. Lets type 'node letter.js start' "); 

//install modules here

var inquirer = require("inquirer"); 
var fs = require("fs"); 
var letter = require("./letter.js"); 





//get CLI commends here, use "process.argv.slice(3).join(" ")" if needed.





// create a constructor that calls a word from wordbank at word.js 
   //create a function game() 
   this.game = function(){


}

